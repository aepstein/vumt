{
  "name": "vumt",
  "version": "0.0.1",
  "description": "Visitor Use Management Tool",
  "main": "server.js",
  "scripts": {
    "client-install": "npm install --prefix client",
    "client-build": "npm run build --prefix client",
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "npm start --prefix client",
    "dev": "concurrently \"NODE_ENV=development npm run server\" \"NODE_ENV=development npm run client\"",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=false npm install --prefix client && npm run build --prefix client",
    "test": "NODE_ENV=test npm run test-unit && npm run test-cucumber",
    "test-cucumber": "NODE_ENV=test npm run client-build && npm run test-cucumber-nobuild",
    "test-cucumber-nobuild": "NODE_ENV=test npx cucumber-js",
    "test-cucumber-wip": "NODE_ENV=test npm run client-build && npm run test-cucumber-wip-nobuild",
    "test-cucumber-wip-nobuild": "NODE_ENV=test npx cucumber-js --tags @wip",
    "test-unit": "NODE_ENV=test mocha --timeout 10000 \"./test/*.js\" \"./test/api/*.js\" \"./test/models/*.js\"",
    "test-unit-wip": "NODE_ENV=test mocha --grep @wip --timeout 10000 \"./test/*.js\" \"./test/api/*.js\" \"./test/models/*.js\""
  },
  "author": "Ari Epstein",
  "license": "GPL-3.0-or-later",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "chai-as-promised": "^7.1.1",
    "concurrently": "^5.0.0",
    "config": "^3.2.4",
    "express": "^4.17.1",
    "express-async-handler": "^1.1.4",
    "factory-bot": "^6.1.4",
    "i18n-iso-countries": "^4.3.1",
    "jsonwebtoken": "^8.5.1",
    "migrate-mongo": "^7.0.1",
    "mongoose": "^5.7.5",
    "mongoose-unique-validator": "^2.0.3",
    "phone": "^2.4.3",
    "postal-codes-js": "^2.2.5",
    "provinces": "^1.11.0"
  },
  "cssnano": {
    "preset": [
      "default",
      {
        "calc": false
      }
    ]
  },
  "devDependencies": {
    "chai": "^4.2.0",
    "chai-http": "^4.3.0",
    "cucumber": "^6.0.2",
    "http-shutdown": "^1.2.1",
    "mocha": "^6.2.2",
    "nodemon": "^1.19.3",
    "puppeteer": "^1.20.0"
  }
}
